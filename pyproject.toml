[project]
name = "irate"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "httpx>=0.28.1",
    "plotly>=6.7.0",
    "polars>=1.40.0",
    "potatodb",
    "pydantic>=2.13.2",
    "shiny>=1.6.0",
]

[tool.uv.sources]
potatodb = { git = "https://github.com/RebarFdn/PotatoDB.git" }
